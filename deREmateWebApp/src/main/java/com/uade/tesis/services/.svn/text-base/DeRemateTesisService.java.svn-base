package com.uade.tesis.services;

import java.util.List;

import com.uade.tesis.domains.Categoria;
import com.uade.tesis.domains.Posting;
import com.uade.tesis.domains.Usuario;

public interface DeRemateTesisService {
	void savePosting(Posting posting);
	Posting getPostingById(Long id);
	Long getNextPostingId();
	List<Posting> getAllPostings();
	
	Categoria getCategoriaById(Long id);
	List<Categoria> getAllCategorias();
	
	Usuario getUsuarioById(Long id);
}
